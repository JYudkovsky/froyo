const userinput = prompt("Please enter your froyo flavors", "vanilla, vanilla, vanilla, strawberry, coffee,coffee")

const carry = userinputer.split(',')

const result = {};

for (const flavor in array) {
if(result[flavor]){
    result[flavor] += 1;
} else {
    result[flavor] = 1;
}
}
console.table(result);










